import { readFileSync } from 'node:fs'; import { resolve } from 'node:path'; import { describe,expect,it } from 'vitest';
const ler=(p:string)=>readFileSync(resolve(process.cwd(),p),'utf8'); const servico=ler('src/services/adminEntregador360.ts'); const pagina=ler('src/paginas/dashboard/admin/AdminEntregadorDetalhe.tsx'); const lista=ler('src/paginas/dashboard/admin/AdminEntregadores.tsx'); const router=ler('src/paginas/dashboard/DashboardRouter.tsx');
describe('Entregador 360 V1',()=>{it('usa apenas RPCs administrativas',()=>{for(const r of ['obter_entregador_admin','listar_veiculos_entregador_admin','listar_documentos_entregador_admin','listar_areas_cobertura_entregador_admin','listar_historico_documental_entregador_admin'])expect(servico).toContain(r);expect(servico).not.toContain(".from('documentos_parceiro_entrega')")});it('expõe rota e CTA sem remover o painel atual',()=>{expect(router).toContain('path="entregadores/:id"');expect(lista).toContain('Ver entregador')});it('usa estados, taxonomia e capacidade reais',()=>{for(const x of ['mota','carro','carrinha','camiao','capacidadeKg','volumeM3','paletes','refrigeracao'])expect(pagina).toContain(x)});it('não expõe paths, foto privada, emergência ou segredos',()=>{for(const x of ['frente_path','verso_path','foto_perfil_url','contacto_emergencia','access_token','refresh_token','service_role'])expect(pagina).not.toContain(x)});it('carrega histórico sob demanda, pagina e mostra versões e eventos reais',()=>{for(const x of ['carregarTab','listarHistoricoDocumentalEntregadorAdmin','DocumentoHistoricoCard','Versão atual','Linha do tempo','Ver frente','Ver verso','totalResultados','tabErro','overflow-x-auto'])expect(pagina).toContain(x)});it('mantém entregas e financeiro indisponíveis',()=>{for(const x of ['Gestão de entregas será disponibilizada','Financeiro logístico ainda não disponível'])expect(pagina).toContain(x)})});
